# 邮箱后缀限制任务清单

- [x] **任务 1：后端预约提交二次校验**
  - 在 `server.ts` 中的 `app.post('/api/reservations')` 路由入口处。
  - 读取 `settings` 中 `allowed_email_suffixes`。
  - 解析配置，提取用户提交 `email` 的域名，与列表中的后缀进行无大小写敏感匹配。若是限制开启却匹配失败，抛出带提示的异常（包含允许的后缀范围）并中止。

- [x] **任务 2：管理端系统常规设置 UI 更新**
  - 在 `src/pages/Admin/components/SettingsTab.tsx` 中增加 `allowed_email_suffixes` 存取逻辑。
  - 提供友好的表单 UI 输入与说明文案（说明无需填写@）。

- [x] **任务 3：客户端预约表单前端拦截**
  - 在 `src/pages/Booking.tsx` 加载时，确保能获取系统级别的后缀配置（例如通过公开的 `/api/settings` 获取，仅做前台基础验证）。
  - 在表单提交 `handleSubmit` 阶段比对邮箱域，若不匹配给予输入框下方清晰的红色提示文案，并务必要展示具体允许的邮箱后缀列表。

- [x] **任务 4：测试与连通性验证**
  - 提供正确的配置，验证预约通过。
  - 提供错误的后缀配置，验证前端报错。
  - 绕过前端调接口，验证后端拦截。
