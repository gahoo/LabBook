- [x] 1. 更新 ReservetionsTab.tsx 中的报告筛选状态选项 (使用 split + 映射)。
- [x] 2. 更新 Mobile 和 Desktop 的状态下拉选项 (修复选项内容，分隔线，添加悬停/激活样式)。
- [x] 3. 简化 Admin/EquipmentManagementTab.tsx 和 Admin/ReservationsTab.tsx 中的联系方式展示格式。
- [x] 4. 将 “待审批白名单申请” 标签放到同行展示。
- [x] 5. 调整白名单的容器 padding (Mobile: px-3 py-2, Desktop: px-2 py-1) 和 gap (从 gap-2 到 gap-1)，提升紧凑感。
- [x] 6. 修复报表筛选器中“时长利用率”标签文案为“时长”。
- [x] 7. 在“本周”、“本月”筛选按钮前增加“今日起”开关，打开时时间区间从当天算起，结束时间自动推延。
- [x] 8. 优化白名单拉取接口，增加 `status=pending` 参数，不再在前端拉取全量数据后过滤。
- [x] 9. 移除 `ReservationsTab.tsx` 中的白名单轮询（`setInterval`），仅在组件加载（即切换到该 Tab）时拉取数据，以减少不必要的接口请求。
- [x] 10. 移除 `Admin/index.tsx` 中的全局白名单状态轮询（`setInterval`），仅在组件加载和切换 Tab 时（依赖 `activeTab`）拉取数据，彻底清理所有的定时轮询。
